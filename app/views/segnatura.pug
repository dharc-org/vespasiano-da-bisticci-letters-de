extends browse-layout

block list-names 
  for x,i in idLetters
   section.dropDown
    h3=x.manuscript
     span= ' [' + x.letter.length + ']'
    p.html Lettere: 
      for y,z in x.letter
        span.letter
         a(href=y.value)= y.value + '   '
  